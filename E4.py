from numpy import pi,sqrt,random,exp
import math
import scipy.stats as sp

#usamos y exponencial
# sacado del github de la materia
c = sqrt(2*exp(1)/pi)
a = (1 / 6 * sqrt(2 * pi))

for x in range(0, 10000):
    y = c * sp.expon(1).rvs()
    y =

def fx(x):
    return a * exp(- ((x - 40)**2) / 72 )

def fy(y):
    return sp.expon(1).rvs()